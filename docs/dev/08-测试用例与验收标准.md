# 08-测试用例与验收标准

## 1. 测试目标

测试目标是验证旅游管理系统是否满足需求规格说明书中的功能要求、权限要求、数据一致性要求和基本性能要求，确保最终提交的源程序可运行、可演示、可验收。

## 2. 测试范围

| 范围 | 是否测试 | 说明 |
|---|---:|---|
| 用户注册登录 | 是 | 核心基础功能。 |
| 权限控制 | 是 | 普通用户不能访问管理员功能。 |
| 景点管理 | 是 | 管理员 CRUD 和用户浏览。 |
| 线路管理 | 是 | 管理员 CRUD、状态和名额。 |
| 订单预订 | 是 | 用户预订、取消、管理员处理。 |
| 评论审核 | 是 | 评论待审核、通过、驳回。 |
| 收藏管理 | 是 | 添加、重复收藏、取消。 |
| 公告资讯 | 是 | 发布、下架、查看。 |
| 数据统计 | 是 | 数量和金额统计。 |
| 在线支付 | 否 | 不在项目范围内。 |

## 3. 测试环境

| 项目 | 配置 |
|---|---|
| 操作系统 | Windows 10/11 或其他开发环境。 |
| 后端 | JDK 17 + Spring Boot。 |
| 前端 | Node.js 18 + Vue 3。 |
| 数据库 | MySQL 8.0。 |
| 浏览器 | Chrome、Edge 或 Firefox。 |

## 4. 功能测试用例

### 4.1 用户与权限

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|---|---|---|---|---|
| TC-USER-01 | 用户注册成功 | 用户名不存在 | 输入合法用户名和密码提交 | 注册成功，生成普通用户。 |
| TC-USER-02 | 重复用户名注册 | 用户名已存在 | 使用相同用户名注册 | 注册失败，提示用户名已存在。 |
| TC-USER-03 | 用户登录成功 | 账号存在且启用 | 输入正确用户名密码 | 登录成功，返回 token。 |
| TC-USER-04 | 密码错误登录 | 账号存在 | 输入错误密码 | 登录失败，提示用户名或密码错误。 |
| TC-AUTH-01 | 普通用户访问后台 | 普通用户已登录 | 访问 `/admin/dashboard` | 被拒绝或跳转首页。 |
| TC-AUTH-02 | 管理员访问后台 | 管理员已登录 | 访问后台首页 | 访问成功。 |

### 4.2 景点管理

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|---|---|---|---|---|
| TC-SPOT-01 | 新增景点 | 管理员登录 | 填写景点名称、分类、票价等提交 | 新增成功，列表可见。 |
| TC-SPOT-02 | 景点名称为空 | 管理员登录 | 名称为空提交 | 保存失败，提示名称不能为空。 |
| TC-SPOT-03 | 修改景点 | 已有景点 | 修改票价或简介 | 修改成功，详情同步变化。 |
| TC-SPOT-04 | 下架景点 | 已有上架景点 | 修改状态为 OFF | 用户端不展示该景点。 |

### 4.3 线路管理

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|---|---|---|---|---|
| TC-ROUTE-01 | 新增开放线路 | 管理员登录 | 填写名称、行程、价格、名额和出发时间 | 新增成功，默认 DRAFT 状态，bookedCount=0。 |
| TC-ROUTE-02 | 名额为 0 | 管理员登录 | 输入名额 0 提交 | 保存失败，提示名额必须大于 0。 |
| TC-ROUTE-03 | 关闭线路 | 已有开放线路 | 状态改为 CLOSED | 用户不可预订。 |
| TC-ROUTE-04 | 公开列表只展示 OPEN/FULL | 存在各状态线路 | 访问 `GET /api/routes` | 仅返回 OPEN 和 FULL 线路，无 DRAFT 和 CLOSED。 |
| TC-ROUTE-05 | 公开访问 DRAFT 线路详情 | 存在 DRAFT 线路 | 访问 `GET /api/routes/{id}` | 返回 404："线路不存在或未开放"。 |
| TC-ROUTE-06 | 管理员查询全部状态 | 管理员登录 | 访问 `GET /api/admin/routes` | 返回所有状态线路，含 DRAFT 和 CLOSED。 |
| TC-ROUTE-07 | 管理员非法 status 查询 | 管理员登录 | `GET /api/admin/routes?status=abc` | 返回 400："状态值只能是 DRAFT、OPEN、FULL 或 CLOSED"。 |
| TC-ROUTE-08 | 修改线路改小 quota | 已有线路 bookedCount=0 | PUT 修改 quota 为更小值 | 修改成功，bookedCount 不超过新 quota。 |
| TC-ROUTE-09 | 删除无订单线路 | 线路无关联订单 | DELETE 删除 | 删除成功。 |
| TC-ROUTE-10 | 未登录访问管理员接口 | 未登录 | 访问 `GET /api/admin/routes` | 返回 401："未登录或登录已过期"。 |

### 4.4 订单预订

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 实测 |
|---|---|---|---|---|---|
| TC-ORDER-01 | 成功预订 | 用户登录，线路 OPEN 且名额充足 | POST `/api/orders` 填写人数和联系人 | 创建 PENDING 订单，totalAmount = price × peopleCount，名额增加。 | 通过 |
| TC-ORDER-02 | 名额不足 | 用户登录，线路 FULL | 提交预订请求 | 返回 400："线路未开放预订"。 | 通过 |
| TC-ORDER-03 | 查询我的订单 | 用户登录 | GET `/api/orders/my` | 返回当前用户的订单列表。 | 通过 |
| TC-ORDER-04 | 查询订单详情 | 用户登录，有订单 | GET `/api/orders/{id}` | 返回订单详情。 | 通过 |
| TC-ORDER-05 | 用户取消订单 | 订单状态 PENDING | PUT `/api/orders/{id}/cancel` | 状态变为 CANCELLED，名额恢复。 | 通过 |
| TC-ORDER-06 | 不能取消已完成订单 | 订单状态 COMPLETED | PUT `/api/orders/{id}/cancel` | 返回 400："当前状态不允许取消"。 | 通过 |
| TC-ORDER-07 | 管理员确认订单 | 存在 PENDING 订单 | PUT `/api/admin/orders/{id}/confirm` | 状态变为 CONFIRMED。 | 通过 |
| TC-ORDER-08 | 管理员驳回订单 | 存在 PENDING 订单 | PUT `/api/admin/orders/{id}/reject` 附 remark | 状态变为 REJECTED，名额恢复，remark 记录。 | 通过 |
| TC-ORDER-09 | 管理员完成订单 | 订单状态 CONFIRMED | PUT `/api/admin/orders/{id}/complete` | 状态变为 COMPLETED。 | 通过 |
| TC-ORDER-10 | 管理员取消订单 | 订单状态 CONFIRMED | PUT `/api/admin/orders/{id}/cancel` 附 remark | 状态变为 CANCELLED，名额恢复。 | 通过 |
| TC-ORDER-11 | 普通用户访问管理员接口 | 普通用户登录 | GET `/api/admin/orders` | 返回 403："无权限访问"。 | 通过 |
| TC-ORDER-12 | 未登录访问订单接口 | 未登录 | GET `/api/orders/my` | 返回 401："未登录或登录已过期"。 | 通过 |
| TC-ORDER-13 | 用户查询非法 status | 用户登录 | GET `/api/orders/my?status=abc` | 返回 400："状态值只能是 PENDING、CONFIRMED、CANCELLED、REJECTED 或 COMPLETED"。 | 通过 |
| TC-ORDER-14 | 管理员查询非法 status | 管理员登录 | GET `/api/admin/orders?status=abc` | 返回 400："状态值只能是 PENDING、CONFIRMED、CANCELLED、REJECTED 或 COMPLETED"。 | 通过 |

### 4.5 评论与收藏

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 实测 |
|---|---|---|---|---|---|
| TC-CMT-01 | 提交景点评论 | 用户登录 | POST `/api/comments` targetType=SPOT | 评论状态为 PENDING。 | 通过 |
| TC-CMT-02 | 提交线路评论 | 用户登录 | POST `/api/comments` targetType=ROUTE | 评论状态为 PENDING。 | 通过 |
| TC-CMT-03 | 非法 targetType | 用户登录 | POST targetType=ORDER | 返回 400："评论类型只能是 SPOT 或 ROUTE"。 | 通过 |
| TC-CMT-04 | 非法 rating | 用户登录 | rating=6 | 返回 400："评分最大为 5"。 | 通过 |
| TC-CMT-05 | content 为空 | 用户登录 | content="" | 返回 400："评论内容不能为空"。 | 通过 |
| TC-CMT-06 | 评论下架景点 | 用户登录，景点状态 OFF | POST targetType=SPOT, targetId=OFF 景点 | 返回 400："景点不存在或已下架"。 | 通过 |
| TC-CMT-07 | 评论未开放线路 | 用户登录，线路状态 DRAFT 或 CLOSED | POST targetType=ROUTE, targetId=DRAFT/CLOSED 线路 | 返回 400："线路不存在或未开放"。 | 通过 |
| TC-CMT-08 | 公开查询只返回 APPROVED | 存在各状态评论 | GET `/api/comments?targetType=SPOT&targetId=1` | 仅返回 APPROVED 评论。 | 通过 |
| TC-CMT-09 | 管理员查询全部评论 | 管理员登录 | GET `/api/admin/comments` | 返回全部状态评论。 | 通过 |
| TC-CMT-10 | 管理员按 status 查询 | 管理员登录 | GET `/api/admin/comments?status=PENDING` | 仅返回 PENDING 评论。 | 通过 |
| TC-CMT-11 | 审核通过评论 | 管理员登录，存在 PENDING 评论 | PUT `/api/admin/comments/{id}/audit` status=APPROVED | 评论状态变为 APPROVED。 | 通过 |
| TC-CMT-12 | 驳回评论 | 管理员登录，存在 PENDING 评论 | PUT status=REJECTED | 评论状态变为 REJECTED。 | 通过 |
| TC-CMT-13 | 重复审核已审核评论 | 评论已审核 | 再次审核 | 返回 400："只能审核待审核状态的评论"。 | 通过 |
| TC-CMT-14 | 普通用户访问管理员接口 | 普通用户登录 | GET `/api/admin/comments` | 返回 403。 | 通过 |
| TC-CMT-15 | 未登录提交评论 | 未登录 | POST `/api/comments` | 返回 401。 | 通过 |

#### 4.5.1 收藏管理

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 实测 |
|---|---|---|---|---|---|
| TC-FAV-01 | 收藏景点 | 用户登录 | POST `/api/favorites` targetType=SPOT | 收藏成功。 | 通过 |
| TC-FAV-02 | 重复收藏 | 已收藏目标 | 再次收藏同一目标 | 返回 409："已收藏该内容"。 | 通过 |
| TC-FAV-03 | 收藏线路 | 用户登录 | POST `/api/favorites` targetType=ROUTE | 收藏成功。 | 通过 |
| TC-FAV-04 | 非法 targetType | 用户登录 | POST targetType=ORDER | 返回 400："收藏类型只能是 SPOT 或 ROUTE"。 | 通过 |
| TC-FAV-05 | 收藏不存在的景点 | 用户登录 | POST targetType=SPOT, targetId=999 | 返回 400："景点不存在或已下架"。 | 通过 |
| TC-FAV-06 | 收藏下架景点 | 用户登录，景点状态 OFF | POST targetType=SPOT, targetId=OFF 景点 | 返回 400："景点不存在或已下架"。 | 待测 |
| TC-FAV-07 | 收藏未开放线路 | 用户登录，线路状态 DRAFT 或 CLOSED | POST targetType=ROUTE, targetId=DRAFT/CLOSED 线路 | 返回 400："线路不存在或未开放"。 | 待测 |
| TC-FAV-08 | 查询我的收藏 | 用户登录 | GET `/api/favorites/my` | 返回当前用户的收藏列表。 | 通过 |
| TC-FAV-09 | 按 targetType 查询 | 用户登录 | GET `/api/favorites/my?targetType=SPOT` | 仅返回 SPOT 类型收藏。 | 通过 |
| TC-FAV-10 | 取消收藏 | 用户登录，有收藏 | DELETE `/api/favorites/{id}` | 取消成功。 | 通过 |
| TC-FAV-11 | 取消不存在的收藏 | 用户登录 | DELETE `/api/favorites/999` | 返回 404："收藏记录不存在"。 | 通过 |
| TC-FAV-12 | 未登录访问收藏接口 | 未登录 | GET `/api/favorites/my` | 返回 401。 | 通过 |

### 4.6 公告与统计

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 实测 |
|---|---|---|---|---|---|
| TC-ANN-01 | 公开查询公告列表 | 数据库有 PUBLISHED 公告 | GET `/api/announcements` | 只返回 PUBLISHED 公告，分页正常。 | 通过 |
| TC-ANN-02 | 公开查看已发布公告详情 | 有 PUBLISHED 公告 | GET `/api/announcements/{id}` | 返回公告详情。 | 通过 |
| TC-ANN-03 | 公开查看 DRAFT 公告详情 | 有 DRAFT 公告 | GET `/api/announcements/{draftId}` | 返回 404："公告不存在或未发布"。 | 通过 |
| TC-ANN-04 | 公开查看 OFFLINE 公告详情 | 有 OFFLINE 公告 | GET `/api/announcements/{offlineId}` | 返回 404："公告不存在或未发布"。 | 通过 |
| TC-ANN-05 | 管理员查询公告列表 | 管理员登录 | GET `/api/admin/announcements` | 返回全部状态公告。 | 通过 |
| TC-ANN-06 | 管理员按状态筛选 | 管理员登录 | GET `/api/admin/announcements?status=PUBLISHED` | 只返回 PUBLISHED 公告。 | 通过 |
| TC-ANN-07 | 管理员非法状态筛选 | 管理员登录 | GET `/api/admin/announcements?status=abc` | 返回 400："状态值只能是 DRAFT、PUBLISHED 或 OFFLINE"。 | 通过 |
| TC-ANN-08 | 管理员新增公告 | 管理员登录 | POST `/api/admin/announcements`，title 和 content 合法 | 返回 DRAFT 公告，publishTime 为空。 | 通过 |
| TC-ANN-09 | 新增公告 title 为空 | 管理员登录 | POST `/api/admin/announcements`，title 为空 | 返回 400："公告标题不能为空"。 | 通过 |
| TC-ANN-10 | 新增公告 title 超长 | 管理员登录 | POST `/api/admin/announcements`，title 超过 100 字 | 返回 400："公告标题不能超过 100 字"。 | 通过 |
| TC-ANN-11 | 新增公告 content 为空 | 管理员登录 | POST `/api/admin/announcements`，content 为空 | 返回 400："公告内容不能为空"。 | 通过 |
| TC-ANN-12 | 管理员修改公告 | 管理员登录，有公告 | PUT `/api/admin/announcements/{id}` | 修改成功，返回更新后数据。 | 通过 |
| TC-ANN-13 | 管理员发布公告 | 管理员登录，有 DRAFT 公告 | PUT `/api/admin/announcements/{id}/publish` | 状态变为 PUBLISHED，publishTime 有值。 | 通过 |
| TC-ANN-14 | 管理员下架公告 | 管理员登录，有 PUBLISHED 公告 | PUT `/api/admin/announcements/{id}/offline` | 状态变为 OFFLINE，publishTime 保留。 | 通过 |
| TC-ANN-15 | 管理员删除公告 | 管理员登录，有公告 | DELETE `/api/admin/announcements/{id}` | 删除成功。 | 通过 |
| TC-ANN-16 | 普通用户访问管理员公告接口 | 普通用户登录 | GET `/api/admin/announcements` | 返回 403。 | 通过 |
| TC-ANN-17 | 未登录访问管理员公告接口 | 未登录 | GET `/api/admin/announcements` | 返回 401。 | 通过 |
| TC-STAT-01 | 查看统计 | 管理员登录，有演示数据 | 打开后台首页 | 显示景点、线路、订单和金额统计。 |

## 5. 非功能测试

| 编号 | 测试项 | 方法 | 通过标准 |
|---|---|---|---|
| NFT-01 | 启动测试 | 按 README 启动前后端 | 无报错，页面可访问。 |
| NFT-02 | 响应速度 | 手工访问常用列表 | 3 秒内返回。 |
| NFT-03 | 权限安全 | 使用普通用户调用管理员接口 | 返回 403 或业务错误。 |
| NFT-04 | 数据一致性 | 创建和取消订单后检查名额 | 名额变化正确。 |
| NFT-05 | 易用性 | 按演示流程操作 | 无明显阻塞或不明确提示。 |

## 6. 验收标准

### 6.1 必须通过

1. 源程序可按运行指南启动；
2. 管理员可以登录后台；
3. 普通用户可以注册登录；
4. 景点和线路可管理、可浏览；
5. 用户能预订线路并生成订单；
6. 管理员能处理订单；
7. 权限控制有效；
8. 数据库脚本可执行；
9. 交付文档完整。

### 6.2 建议通过

1. 评论审核流程完整；
2. 收藏功能可用；
3. 公告发布和下架可用；
4. 后台统计数据正确；
5. 项目总结报告包含真实测试结果。

## 7. 缺陷记录模板

| 缺陷编号 | 发现时间 | 模块 | 描述 | 严重程度 | 负责人 | 状态 | 修复说明 |
|---|---|---|---|---|---|---|---|
| BUG-001 | YYYY-MM-DD | 订单 | 取消订单后名额未恢复 | 高 | 成员 A | 待修复 | 待填写 |

## 8. 测试结论模板

```markdown
本轮测试共执行 X 个用例，通过 X 个，失败 X 个，阻塞 X 个。核心功能已/未满足验收要求。遗留问题包括：……。经项目组讨论，……问题已修复或作为后续改进项记录。
```
